/**
 * Common TTL (Time to Live) values in milliseconds.
 */
export enum TTLCommon {
  ONE_MINUTE = 60000,
  FIVE_MINUTES = 300000,
  TEN_MINUTES = 600000,
  HALF_HOUR = 1800000,
  ONE_HOUR = 3600000,
}
